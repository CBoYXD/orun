[project]
name = "orun"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.6.1",
]

[project.scripts]
orun = "orun.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
